name: using-reusable-workflows

on:
  workflow_dispatch:
    inputs:
      name:
        description: "who"
        type: string
        default: 'mallen'
        required: true

jobs:
  calling-a-reusable-workflow:
    uses: allenhub-net/TCGAaWGuide-a/.github/workflows/reusable.yaml@0ba0ebe622192d7b99b713228228c9398c450bfb # v1.1.8
    with: 
      name: ${{ inputs.name }}
    secrets:
      access-token: ${{ secrets.ACCESS_TOKEN }}

