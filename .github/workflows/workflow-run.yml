name: workflow-run

on: 
  workflow_run:
    workflows:
      - events
    type:
      - completed

jobs:
  echo-string:
    runs-on: ubuntu-latest
    steps:
      - run: echo "::notice title=I was triggered because 'events' was completed"
