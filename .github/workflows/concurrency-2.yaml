name: conc2

on:
  workflow_dispatch:
    inputs:
      environment:
        description: Environment
        type: environment
        required: true

jobs:
  sleep:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "::notice title=start at $(date)::"
          sleep 30

        